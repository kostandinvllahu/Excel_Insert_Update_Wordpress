To use this code on wordpdress you must install a php plugin, and you then you just call it in your wordpress html form.

The prefered format of excel is .csv, if the code doesnt work check the format inside the values they must be seperated with ","

check the excel file sample i have uploaded.

The plugin is "Insert PHP Code Snippet" install it and follow its tutorial!